import { api, client, hasError, getUserFacilities, initialise, resetConfig, subscribeTopic, unsubscribeTopic, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  getUserFacilities,
  initialise,
  hasError,
  resetConfig,
  subscribeTopic,
  unsubscribeTopic,
  updateInstanceUrl,
  updateToken
}