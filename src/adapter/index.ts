import { api, client, getConfig, hasError, initialise, logout, resetConfig, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  getConfig,
  initialise,
  logout,
  hasError,
  resetConfig,
  updateInstanceUrl,
  updateToken
}